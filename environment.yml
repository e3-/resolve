name: resolve

channels:
  - conda-forge
  - gurobi

dependencies:
  - awscli=2.27.55
  - gurobi>=12.0.0
  - kubernetes=1.24.3
  - pip=26.0.1
  - python>=3.11,<3.12
  - setuptools=66.1.1

  - pip:
      # Install current repo as editable package
      - -e ".[kit,jupyter,ui]"
